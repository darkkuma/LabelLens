{
  "name": "labellens",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check data/products.js && node --check scripts/collect-kurly-labels.mjs && node --check worker/index.js && node scripts/validate-products.mjs"
  }
}
